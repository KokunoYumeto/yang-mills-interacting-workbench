# Complete fourth source, full physical return, and sixth vacuum energy

Read `QUARTIC_SOURCE.md`, `PHYSICAL_RETURN.md`, and `SIXTH_ENERGY.md` in that order.
The original SU(2) Hamiltonian, physical parameters, supports, raw state pairings,
all scalar energies and every intermediate Casimir remain attached.

The continuation evaluates all 8,621 anchored connected fourth-order multisets
through 78 explicit signed-coordinate transports. Their 743 trace-monomial
coefficients, full original words, coefficient bounds and linear-program duals
are rebuilt by the dependency-free Python producer. The improved physical gap
holds on the complete original finite-volume physical form domain. The exact
sufficient g-squared threshold is approximately 3.715960362535436, with the
rational bracket retained. The fourth total-/point-spin bounds are also evaluated and used in the full correction at q4, with all residual degrees five through eight retained. The complete sixth energy includes the six-face
cube coefficient -83/1944, independently evaluated over all 720 face orderings.

## Replay

From this directory, with the unchanged sibling `20260916-cubic-linearized`
present:

```sh
python -B verify.py --output-dir generated
python -O -B verify.py --verify-receipt generated/verification.json
python -B replay.py
```

The owner-facing package includes all generated full JSON tables and the full
receipt. Hash equality of all source inputs and the five full generated tables is checked on replay. Every requirement uses an explicit
exception, so optimization removes no tests. The tables and receipts are finite
certificates; the Banach-space, elliptic and spectral arguments are written
proofs, not additional claimed formalizations.

`state.json` records the completed scope and next original quantity.
`SOURCE_INTAKE.json` keeps inherited proof and external-literature scopes.
This is an additive continuation based on unmerged PR8 at
`91434b6962062bd80439d4cb2cae9d2479264dde`; the archive does not imply a merge,
remote publication, paid-model run, or scheduled research.

A nontrivial four-dimensional continuum field and a finite positive continuum
mass remain unestablished. The running path eventually leaves the stated
strong-coupling domain. All dependence on a and g remains in the proofs.
