# Route selection after the volume and complement calculation

21 September 2026. The current result replaces the inherited heat Cauchy radius1/M by the fixed source radius3/256, gives the whole-matrix rather than entrywise tail, and estimates the entire surrounding physical space. These are the two quantitative losses identified in the preceding checkpoint. The completed fourth-degree time functions remain inputs; this tranche does not start another independent coefficient catalogue.

## An evaluated restart, rather than a prospective assertion

Retain the original exact source equation v=xi v1+B(v,v), ||v1||loc<=32 and ||B||<=2/3. For the actual source v_* at 0<xi_*<R0=3/256, the explicit convergent construction gives

    ||v_*||loc<=r_*=(3/4)(1-sqrt(1-256xi_*/3)).

Changing the original coupling coordinate to xi_*+delta, while retaining the same physical operator return, gives the exact equation

    [I-2B(v_*,.)]w=delta v1+B(w,w),   w=v(xi_*+delta)-v_*.

The complete Frechet derivative, original scalar equation and bilinear term remain. Put c=2/3. The inverse estimate obtained solely from the displayed source norm is (1-2c r_*)^(-1). Its complete nonlinear discriminant is

    (1-2c r_*)^2-4c*32 |delta|
       =1-(256/3)xi_*-(256/3)|delta|.

Hence that specific certificate supplies exactly

    |delta|<=R0-xi_*,   xi_*+|delta|<=R0.                  (R1)

At the concrete original reference xi_*=1/1024 its radius is11/1024 and its right endpoint is3/256 again. This is an evaluation of the sufficient certificate, not a statement that the actual source becomes singular at that endpoint. The proof does not promote a triangle bound to the true analytic radius.

For a finite rational check of the symbolic identity, take q in(0,1), xi_*=R0(1-q^2), r_*=(3/4)(1-q). Then 1-2c r_*=q and delta_max=R0q^2, so xi_*+delta_max=R0 identically. route_check.py evaluates this identity at a declared rational family and retains the original xi_*=1/1024 square11/12. The algebraic derivation above applies throughout the displayed domain.

## Selected next original quantity

A useful subsequent continuation must therefore compute more than the scalar restart R1. The available complete fifth source supplies the specific original reference

    q5=xi v1+xi^2 v2+xi^3 v3+xi^4 v4+xi^5 v5,
    J5=2B(q5,.),
    R5=sum_(1<=i,j<=5, i+j>=6) xi^(i+j) B(v_i,v_j).

The next selected evaluation is the signed, same-support action of J5 and the complete R5 in the original point-spin and energy metrics, followed through the Poisson-row identity U27–U29 and the full complement minimum M17. The existing 662-representative fifth catalogue is available in this cumulative archive. The separately described sixth catalogue remains unavailable; it is not assigned a recovered status or silently recreated by a smaller model.

Every scalar removed by Q_H must return to the original E0 and every response must return in the actual physical time tau=kappa t. The previously constructed link-refinement defect and the running parameter c_n=g0^(-2)+beta n log2 remain explicit. The current domain is c_n<sqrt(3)/8; the native numerical families in M10 have their separately stated g^2 lower endpoints. For beta>0 the standing shrinking-g path eventually leaves these domains. A finite continuum mass and a nontrivial smooth four-dimensional field are not established by this contribution.

The new volume, original-metric and complementary-space bounds are completed inputs for that next calculation. Their proof status is written analysis plus the separately scoped exact checks. No paid model run, background continuation, or repository merge is active.
