# Complete zero-shift / full physical gap delivery

The complete mathematical reader is `MATHEMATICAL_READER.md`. The final
strongest theorem is H1–H23, with the three other full proof bodies retained.
The workbench entry is `yang-mills/research-control/CURRENT.md`.

The full physical strong-coupling bound applies to g²>=15, any spacing a>0
and every original open box L>=2. The actual zero-shift response is enclosed;
the unique spatial vacuum/dynamics limit is proved at fixed spacing in that
same coupling range. No smooth four-dimensional continuum mass-gap conclusion
is claimed.

This package includes all selected predecessor bodies and exact helper files
needed for the local replay. It contains no private conversation, credentials,
font file, new paid model execution, or downloaded literature corpus.

Run from this directory:

```sh
python -B yang-mills/continuations/20260915-uniform-gap-zero-shift/replay.py
```

`review.patch` is the combined patch against remote PR6 revision
`e98b2c3af77f66fb1c1396143ca53daef586404f`, including the earlier delivered
actual-loop-moment contribution. `incremental.patch` instead applies after
that earlier delivered patch. Use exactly the patch corresponding to the
starting source. Both were independently applied to fresh selected source
trees and replayed in ordinary and optimized Python. No inherited proof or
receipt is modified; only CURRENT.md is updated among the old tracked files.

The patch construction used local temporary Git snapshots of the exact
selected paths, not invented remote commits. The original CURRENT.md Git
blob is recorded in DELIVERY.json. The proposed PR description is included;
no new remote PR was opened or merge performed.

MANIFEST.json records every included file except itself. Hashes establish
file identity, not analytical correctness. The four complete written proofs
and their stated scope remain the mathematical evidence.
