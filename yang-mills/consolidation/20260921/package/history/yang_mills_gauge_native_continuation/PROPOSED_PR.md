# Gauge-constrained source continuation, the full physical band, and closed-endpoint volume dynamics

## Mathematical contribution

This additive contribution preserves the complete PR6 base and the two subsequent owner-delivered actual-loop/uniform-gap sources. The new proof acts on the original three-spatial-dimensional SU(2) Hamiltonian, with kappa=2g^2/a, xi=1/(4g^4), actual positive vacuum, all plaquette words, raw state metric and all source labels retained.

The exact vertex-gauge constraint gives c(j)>=6j_e. It improves the full nonlinear source and returns the physical free Casimir three to a bound on the whole physical form domain. The actual second vacuum coefficient is then evaluated, including both shared-edge channels; its original Casimir trace-norm bound is 236 rather than 2048/3. The resulting final closed domain is

    g^2>=sqrt((32+sqrt(354))/3)=4.1156161030...,
    Delta_L >= (3 kappa/2)(1+sqrt(1-256xi/3+10720xi^2/9)).

All constants are independent of the original exterior box. A direct uniform-in-volume local-observable mixing estimate and exact coupling modulus prove whole-sequence spatial vacuum/dynamics convergence, including the new source-majorant endpoint. The limiting semigroup has a unique invariant probability and the same physical gap. The comparison of both physical coefficients retains the same original time.

The entire first physical plaquette band has the exact second coefficient T_L=7I/15-(D_degree+A_adjacent)/21, with each open-boundary degree retained. A contour bound supplies the full analytic tail kappa*(6713/39875)*(320|xi|)^3/(1-320|xi|). The original L=2 matrix has 240 faces. Its integer power bounds and complete fraction-free inertia, together with that all-order tail, give the actual first-excitation interval at xi=1e-10. No spin-truncated operator is substituted. The original band metric and its inverse coordinate map are explicit.

Dahmen's planar coefficient was checked against its original definitions and diagrams through the full energy/coupling dictionary B30; its antecedent credit is retained. No historical-priority claim is made.

## Execution and scope

The new standard-library verifier passed 642 uniquely named exact checks and 27 false-formula controls. The full replay includes both Python modes, the unchanged predecessor, a fresh copied source, 20 named CLI corruption rejections and restored-source runs. The original 240-face certificate retains every pivot and 2,303,960 exact integer divisions. All full receipts and executable sources are included.

The analytic proofs remain written arguments for independent review. No new Lean or peer-CI replay, sampled vacuum, automatic merge or paid model execution is claimed. On the standing running path the domain is c_n^2<=3(32-sqrt(354))/670; a positive beta eventually leaves it. A nontrivial smooth four-dimensional continuum field and a finite positive continuum mass remain unevaluated.

The next selected original source is the connected order-three coefficient and full linearized residual at the actual first two coefficients. The current coupling, second-source, band and volume results are completed inputs.

## Publication

This is a proposed PR description, not an assertion that a remote PR was opened. The cumulative patch targets `e98b2c3af77f66fb1c1396143ca53daef586404f`; the incremental patch follows the previous uniform-gap delivery. Existing source and receipt bytes are preserved, except the explicit active `CURRENT.md` pointer update. Repository-wide generated catalogues are left unchanged.
