# Ready-to-paste prompt for the original Yang–Mills web session

The text below is a prepared follow-up for the user to send. It is not an instruction executed by this consolidation.

---

Your Yang–Mills work from 14–16 September has been consolidated at:

https://github.com/KokunoYumeto/yang-mills-interacting-workbench/tree/main/yang-mills/consolidation/20260916

Read its `SOURCE_COVERAGE.md`, `CURRENT_RESEARCH.md` and `VALIDATION.md`, then the actual full sources needed for your next calculation. The consolidation includes the earlier GitHub source chain, actual-loop moments, uniform-gap/zero-shift proofs, gauge-native band and second-source/spatial-return proofs, the full published PR7 local-fibre proof, and the complete cubic/linearized delivery with its full verification receipt. The gauge-native archive already subsumes the earlier actual-loop mathematical files. The two cubic ZIP downloads are identical. The file delivered as `ZETA_SEVEN_OBSERVABLE_20260916.patch` is actually an exact copy of your cumulative gauge-native Yang–Mills patch, also integrated. Do not ask for these already-covered deliveries again.

First recover the following **still-missing original files**, checking your existing session filesystem and artifacts before recreating anything:

1. **Initial Split Zero delivery:** `split_zero_yang_mills_research.zip`, including the full `split_zero_ym/RESEARCH_NOTE.md`, `exact_certificate.py` and `certificate.json`. The complete final mathematical response was recovered, but these separately linked files were not.
2. **Smooth-continuum delivery:** `yang_mills_smooth_continuum_continuation.zip`, `yang_mills_smooth_continuum_note.md`, its complete verifier/results and `yang_mills_smooth_continuum.patch`. Again, the visible final response is present; the separately linked full attachment is absent.
3. **Later gauge-resolved delivery:** `yang_mills_gauge_resolved_gap.zip`, including `yang_mills_gauge_resolved_gap.md`, `yang_mills_gauge_volume_resolvent.md`, the full `yang_mills_gauge_verification.json`, execution/patch-validation records and associated patches. Its complete visible response survives, but not those source attachments. Preserve this as its historical first-source result; it must not overwrite the stronger saved second-source result.
4. **PR7 local-fibre verification package:** `yang_mills_zero_shift_local_fibres.zip`, including the complete executable checker, every imported dependency and the full decoded receipt. The full 1,031-line proof and README are already integrated. Their proof SHA-256 is `42aa9ab14a4b42ba9d170803fb0f9ce9f0d1d763d10139b3a15f9ba317543e55`; the expected full decoded receipt SHA-256 recorded there is `3b68db08b411f55c39c0b3f3cd7d1401c321fded1cc0eba3250037e8dcca9e23`. Recover and compare the actual files rather than representing the historical statement of 408 checks as a fresh replay.

Return recovered original files unchanged, with path/byte-count/SHA-256 manifests and their actual provenance. If an original is gone, say exactly which file is gone. Any new reconstruction must be labeled as a reconstruction, with its new proof and fresh checks; do not fabricate an old receipt or claim the original attachment was recovered. The consolidation already records minor rendering and form-domain qualifications, so read the relevant bounded audit before preparing replacements.

After recovering and packaging the missing sources, the relevant unfinished continuation is the **connected fourth vacuum-source coefficient**

`v_4 = 2 B(v_1,v_3) + B(v_2,v_2)`

and the signed, channel-resolved action of the actual linearized operator `J_2 = 2 B(xi v_1 + xi^2 v_2, .)` on those same original supports. The last session messages announced this work but delivered no completed four-face table or new certificate. The complete cubic source, the quartic residual formula, the fourth **vacuum-energy** coefficient and its analytic tail are already completed inputs; do not present them as a newly completed fourth vacuum-source calculation.

Continue from the original oriented lattice coordinates, gauge action, source labels, Haar and actual-vacuum measures, scalar energy, raw Grams and parameters `kappa=2g^2/a`, `xi=1/(4g^4)`. Compute the complete signed contributions on each connected four-face support before taking absolute values. Prove the support classification, all channel coefficients and denominators, and the exact map back to the original physical operator and primitive norm. Retain the full residual, nonlinear tail and endpoint argument when returning an improved estimate. A few exact quaternion assignments per support are regression checks, not a universal polynomial-identity proof.

Keep the distinct result domains explicit. The cubic/linearized proof supplies a finite-lattice physical gap uniformly in `L` at `g^2 >= 1/(2 sqrt(alpha))`, approximately `3.825973052393386`, with `alpha` defined by its actual quartic. The saved infinite-volume uniqueness proof has the earlier threshold `g^2 >= sqrt((32+sqrt(354))/3)`, approximately `4.115616`. Do not extend uniqueness to the cubic threshold without supplying the actual extension proof. A nontrivial four-dimensional continuum field and a finite positive continuum mass are not supplied by these strong-coupling statements. State and prove the precise new result you obtain; do not assume an unresolved physical conclusion as a theorem hypothesis.

Deliver **one genuinely cumulative source package** containing all available earlier mathematical source and verification dependencies plus the recovered/new files, a complete current reader with editable source, an exact manifest, fresh execution records, and an explicit missing-file list if anything remains unavailable. A separate incremental patch is useful in addition, but must be labeled incremental and name its exact base commit. Ensure the newest cumulative ZIP can be used without guessing which older downloads it needs. Treat this as a concrete mathematical continuation and source-recovery task, not merely a plan or a progress-message response.
