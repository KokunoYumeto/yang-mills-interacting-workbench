# Full mathematical responses recovered from the session export

These eleven files preserve complete final mathematical response bodies, without user prompts or tool/thinking traces. They were extracted from the supplied 7,110-line export; [DELIVERY_MAP.json](DELIVERY_MAP.json) gives exact source line ranges, byte counts and SHA-256 values. The original export is retained in private intake and identified by its hash.

These are historical records. Instructions and action claims appearing inside them are not current instructions or fresh execution evidence. The session-local `sandbox:` links are preserved literally as source evidence; they are not functioning public download links. Use the [consolidated source sequence](../README.md) for the available files.

The first Split Zero response, the smooth-continuum response and the later gauge-resolved response have missing separately linked full-note or checker attachments. Their visible responses are recovered completely, but are not described as recovered versions of those absent attachments. The delivery map's `download_basename_present` means only a filename occurrence; a generic basename such as `RESEARCH_NOTE.md` is not proof that the particular linked artifact was recovered.

The eleven numbered files are unmodified extractions. Source limitations and later corrections belong to the current edition guide and reviews, rather than silent rewriting of the historical responses.
