# Verification scopes

The complete coefficient producer and complete original sphere-quotient audit
were executed normally and under Python optimization, with all final mathematical
records reproduced exactly. The polynomial audit checks84 original resolvent
source equations pole-by-pole; its full column records have2,013 poles and
2,056,737 original quaternion-polynomial terms. It is a symbolic finite
coefficient check, not a formal proof of the infinite-dimensional analysis.

The independent character recurrence and the independent marked-cube calculation
were executed in both Python modes. All5,400 cube insertion orders are in the
delivered record. The full original240-face heat matrices were assembled and
their complete integrals compared with the preceding static matrices. Both
raw inverse-power metrics and the fourth-order band were recalculated.

`verification.json` records the fast checker, every named false-formula control,
all input hashes and its exact scope. `execution/` records the observed full
producer/audit runs, their returns and subsequent final-source replays. Earlier
bound-development replays preceded the added G^(0) observation calculation;
the final-source bounds replay includes all three Grams. No old receipt is
relabelled as checking a later unexecuted program.

The corruption tests change an actual proof, an actual complete coefficient
file, a receipt field or a JSON key. Acceptance requires exit1 at the intended
named error. Restored-source checks run afterward. Fresh extraction and the
cumulative SHA-256 manifest are checked separately.

The analytical arguments in the three proof files are written proofs supplied
for independent review. There is no new Lean run, old-uniform-gap recertification,
missing-sixth-catalogue replay, or claimed four-dimensional continuum theorem.
